# contact_independent_address_odoo
Stops Odoo from automatically copying a parent company address onto child contacts, and from pushing a child contact address back to the parent company. This allows each child contact to maintain its own address independently.
